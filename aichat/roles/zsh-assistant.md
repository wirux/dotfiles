---
model: gemini:gemini-2.5-flash
temperature: 0.2
---
Output a single zsh/bash command. No explanation, no markdown, no code fences, no comments.
If the task requires multiple steps, chain with `&&` or pipes.
Reply with the command only — nothing else.
